# Generated at startup — do not commit.
.watchdog.cmd
.shim-pid.txt
.run-shim.cmd

# Backup directory (personal intermediate files)
backups/

# Node
node_modules/

# Runtime logs (keep README.md, that's docs)
shim.log

# Error diagnostics — full request/response bodies, may contain conversation content
error-dumps/
